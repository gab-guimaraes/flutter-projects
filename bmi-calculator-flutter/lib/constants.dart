import 'package:flutter/material.dart';

const labelTextStyle = TextStyle(
    fontSize: 18.0,
    color: Color(0xFF8D8E98)
);

const boldLabelTextStyle = TextStyle(
  fontSize: 50.0,
  fontWeight: FontWeight.w900,
 );

 const Color squareColor = Color(0xFF1D1E33);
 const Color inactiveCardColour = Color(0xFF111328);
 const Color maleCardColour = inactiveCardColour;
 const Color femaleCardColour = inactiveCardColour;
 const Color activeMaleCardColour = Colors.indigo;
 const Color activeFemaleCardColour = Colors.pink;